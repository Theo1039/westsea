# Django secret key — keep this secret and do NOT share publicly
SECRET_KEY=!_mv_qj(bovw*4fixce3*96+h9*%_#y4%shthgl05x@-ok(2wh

# Debug mode — set to False in production!
DEBUG=False

# Allowed hosts — comma-separated list without spaces
ALLOWED_HOSTS=127.0.0.1,localhost,westsea.onrender.com

# Email configuration for Gmail SMTP
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=theophiluseckpurugbara@gmail.com

# Your Gmail app password (keep secret!)
EMAIL_HOST_PASSWORD=iqknduphriqxktnv

# Database URL — use Postgres on Render for production
# Example format: postgres://user:password@host:port/dbname
DATABASE_URL=sqlite:///db.sqlite3
